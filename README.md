# ress vault

This directory is a snapshot of an Omarchy machine, written by
[ress](https://github.com/btsouth/omarchy-resurrect).

Replay it onto a fresh install with:

```bash
ress restore --vault /home/samiul/.local/share/ress/vault
```
